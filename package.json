{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "corepack prepare pnpm@9 --activate && node -e \"const fs=require('fs'); for (const f of ['package-lock.json','yarn.lock']) { try { fs.rmSync(f,{force:true}); } catch(e) {} }\"",
    "build": "node -e \"const { execSync } = require('child_process'); const buildProd = () => execSync('corepack pnpm --filter \"./artifacts/api-server\" run build', { stdio: 'inherit' }); const buildDev = () => execSync('corepack pnpm -r --if-present run build', { stdio: 'inherit' }); if (process.env.NODE_ENV !== 'production') { execSync('corepack pnpm run typecheck', { stdio: 'inherit' }); buildDev(); } else { buildProd(); }\"",
    "typecheck:libs": "corepack pnpm exec tsc --build",
    "typecheck": "corepack pnpm run typecheck:libs && corepack pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "typescript": "~5.9.3"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
